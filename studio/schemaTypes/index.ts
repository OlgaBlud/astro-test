// import {postType} from '../schemas/postType'
import customImage from '../schemas/customImage'
import normalizedText from '../schemas/normalizedText'
import siteSettings from '../schemas/siteSettings'
import difference from './objects/difference'
import hero from './objects/hero'
import home from './pages/home'

export const schemaTypes = [siteSettings, normalizedText, customImage, home, hero, difference]
